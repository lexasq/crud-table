import { DEPARTMENTS_NAME, departmentsReducer } from './reducers/departments.reducer';

export const Reducers = {
  departments: departmentsReducer
};

export const NAMES = {
  departments: DEPARTMENTS_NAME,
};
